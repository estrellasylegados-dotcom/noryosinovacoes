# Estratégia

> O que importa agora. Prioridades, metas, prazos.
> O Claude usa isso pra decidir o que sugerir primeiro e o que adiar.
> Atualize sempre que as prioridades mudarem.

## Fase

Pós-lançamento da Noryos Inovações — o site institucional está no ar em https://noryosinovacoes.com.br (deploy Hostinger Web Apps, automático a partir do `main`). Construído e testado (build + lint verdes). Em 28/08/2026 passou por um redesign profundo da camada visual ("Digital Operating System": novo hero, tipografia, motion system, cards, Noryos OS Explorer, diagnóstico visual) — estratégia, SEO e arquitetura preservados. Em 29/08/2026: auditoria + ajuste do motion system (reveals agora perceptíveis no scroll — o gatilho disparava no rodapé) e logo/favicon/Open Graph oficiais plugados. Segundo passe no mesmo dia: presença da logo no header (28→52px no desktop) + favicon robusto (`favicon.ico` 16/32/48, `icon.png` 512, `apple-icon` 180, gerados de `noryos-icon.png`). Terceiro passe (29/08/2026): `noryos-logo.png` trocado pela 2ª entrega oficial do wordmark e Header desktop passou a travar a largura em 170px (testados 160/170/180) — commit `66ec398`. Em 29–30/08/2026: instalada a 2ª camada do Quality Gate — suíte Playwright E2E no `site/` (`npm run quality` = lint + build + E2E; roda contra o build de produção). Em 30–31/08/2026: teste de conceito completo do endpoint do Diagnóstico Digital — `POST /api/diagnostico` (route handler no lugar da Server Action) com pipeline anti-spam (honeypot, tempo mínimo, rate limit, limites de payload/método/Content-Type, dedupe), persistência antes da notificação, e notificação interna por e-mail via Resend (`fetch`, sem SDK; provider `mock` pros testes). Rate limit agora tem backend compartilhado via Supabase RPC (fallback em memória) e o fallback local de persistência foi desligado em produção (503 controlado, sem gravar em disco). Suíte E2E subiu pra 52 testes. Commitado como trabalho em andamento. Em 01/09/2026: projeto Supabase real criado (tabelas `diagnosticos` + `diagnostico_rate_limit` + RPC `diagnostico_check_rate_limit` rodadas via SQL, confirmadas por SSH), env vars Supabase e Resend configuradas na Hostinger (key do Supabase no formato novo `sb_secret_…`), domínio `noryosinovacoes.com.br` verificado no Resend, e deploy de produção concluído. **Porém `POST /api/diagnostico` ainda responde 503 em produção** — a persistência falha e a causa raiz da conexão backend↔Supabase está sob investigação (não é mais "criar o projeto"). Instrumentação de log publicada (commit `97de1b4`: logs estruturados `supabase_insert_failed` / `supabase_client_absent` / `rate_limit_degraded` + `site/scripts/supabase-doctor.mjs`), aguardando deploy na Hostinger pra revelar a causa. Em 02/09/2026: Cloudflare Turnstile obrigatório server-side no `POST /api/diagnostico` (validação via `siteverify` antes de persistir/notificar; token ausente/inválido → 403 sem gravar nem enviar e-mail; secret só no servidor, site key público via prop do Server Component) e rate limit passou a ter **duas janelas fixas por origem** (3 envios / 15 min + 10 / 24 h, mesma RPC/tabela Supabase). Testado localmente e commitado (`32641cc`). **Ainda em 02/09/2026: 503 resolvido + Diagnóstico Digital V2 com scoring comercial V1** — `site/src/lib/diagnostico-scoring.ts`, qualificação determinística sem IA: maturidade digital e potencial comercial (0–100), classificação por faixa, prioridade, gaps, serviços Noryos recomendados, próxima ação; `scoring_version = "v1"`; colunas novas (`maturidade_digital` / `classificacao` / `prioridade` / `scoring_version` + índices) via migração aditiva já aplicada no Supabase de produção. **Validado ponta a ponta em produção** (commits `219c35c` + `0bbff35`): envio real com linha completa no Supabase, e-mail interno com resumo comercial (subject neutro <85), teste negativo (POST sem Turnstile → 403, nada gravado). Suíte E2E em **78 testes**. **Quality Gate: APROVADO — VALIDADO EM PRODUÇÃO.** Em 03/09/2026: **Formulário V2 do Diagnóstico** — coleta estruturada (chips/cards) no lugar de texto livre, **sem recalibrar o scoring** (`scoring_version` segue `"v1"`; `form_version = "v2"`); ponte pura `composeLegacyText` (Form V2 → scoring V1, `site/src/lib/diagnostico-compose.ts`); decisão U1 (`prazo` alimenta a urgência do V1 via a composição); colunas aditivas no Supabase (`form_version` / `respostas` jsonb / `prazo` / `objetivo_principal` / `porte`, migração `site/supabase/migrations/2026-09-03_diagnostico_form_v2.sql`); e-mail com bloco "Respostas do lead (estruturado)"; funil do formulário instrumentado (`site/src/lib/analytics.ts` + `<Analytics/>` guardado por env — **sem PII**). Investimento ficou **fora** desta versão; objetivo principal e prazo são **obrigatórios**; porte é **opcional**. Commit `376a8c6`. Suíte E2E em **108 testes**. **VALIDADO EM PRODUÇÃO em 03/09/2026** — migração aditiva aplicada no Supabase (5 colunas + 3 índices), deploy Hostinger + purge de CDN, envio real conferido ponta a ponta (`form_version='v2'`, `scoring_version='v1'`, scoring + `respostas` jsonb coerentes, colunas legadas preenchidas, e-mail com o bloco estruturado), leads antigos intactos (`form_version` NULL, nenhuma linha alterada), teste negativo ok (POST sem Turnstile → 403 sem gravar, `GET` → 405). **QUALITY GATE: APROVADO.** Em 03/09/2026 (commit `a8b7bdc`): passe de correção de motion/layout na home, **VALIDADO EM PRODUÇÃO** (deploy Hostinger sem purge manual, conferido na URL limpa). (1) A linha de progresso do fluxo "como sistema" e a de "Como trabalhamos" passaram a **completar em posição de leitura** — remapeadas `progress 0,12–0,52` (era `0,15–0,75`, os passos Dados/Evolução nunca acendiam) — e deixaram de **congelar num valor parcial em scroll rápido**: `useScrollProgress` agora resolve pros extremos (0/1) ao sair da viewport. (2) Label "Relacionamento" não é mais **cortado** pelo `clip-path: inset(0)` que `[data-anim].is-in` aplica na coluna (`min-w-0` + quebra com hífen). (3) Card "Presença Digital" das Soluções **sem o vazio interno de ~400px** (removido `lg:items-stretch` + `h-full` + `grow` forçado). (4) Placeholders do card "Conteúdo" trocados por mocks compactos (não parecem mais skeleton travado). (5) Barras do mini-gráfico de Aquisição repousam com forma de gráfico (~45%, era 6% = linha rasa). (6) 6º card **"Transparência"** fecha a lacuna da grade "Por que Noryos" (3×2). `npm run quality` verde (108 E2E) + inspeção visual 1440/390/reduced-motion, console limpo. **QUALITY GATE: APROVADO.** Em 04/09/2026: **redesign dos gráficos das seções da home em andamento** (crítica + direção aprovadas pelo usuário — "instrumentos de uma operação": ciano=fluxo/ativo, verde=confirmação, sem lib de animação, um ciclo por vez validado antes de replicar). **1ª peça entregue e validada localmente — Automação (`AutomationConveyor`)**: a lista estática virou esteira viva (token percorre Lead→WhatsApp→CRM→Follow-up, cada etapa acende quando o pulso chega, microstatus por etapa, ciclo ~8,3s com repouso ~2s e reset suave; hover mostra tooltip sem parar o ciclo; horizontal só ≥1180px, vertical abaixo; reduced-motion = estado final conectado estático). `AutomationFlow` removido. `npm run quality` verde (**118 E2E**, +6 novos: ciclo/estágios/reset/overflow/mobile/reduced-motion + breakpoint) + inspeção visual 1440/1280/1180/1024/768/390/reduced-motion. **QUALITY GATE: APROVADO — VALIDADO LOCALMENTE** (sem deploy). **2ª peça — Aquisição e Performance (`PerformancePanel`)**: `MiniChart` (8 barras aleatórias + rótulo "Ilustrativo") virou painel de leitura — duas linhas (investimento vs. retorno em **índice relativo base 100, sem valor absoluto/moeda**), margem preenchida, 3 indicadores direcionais com sparkline (variação % + seta, verde=positivo), ponto de decisão, rótulo "Exemplo de acompanhamento · sem dado de cliente". Mesma linguagem da esteira (idle→atividade→progresso→confirmação) **sem esteira**: nada viaja; ao entrar na viewport (`useInView` latch) as linhas acendem, a margem preenche L→R, indicadores e ponto de decisão entram; 1 ponto pulsa na ponta (repouso). Hover (desktop) = scrubber por semana com índice. Repouso lê como gráfico (linhas presentes, discretas). reduced-motion/sem-JS = traçado completo estático. `MiniChart` removido. `npm run quality` verde (**128 E2E**, +10 novos: repouso→revela/latch/hover/sem-moeda/larguras/reduced-motion) + inspeção visual 1440/1280/1024/768/390/hover/reduced-motion. **QUALITY GATE: APROVADO — VALIDADO LOCALMENTE.**

**3ª peça — "Operação fragmentada → conectada" (`OperationShift`, §2/§3 fundidas numa narrativa só)**: `FragmentGrid` + `SystemFlow` viraram uma peça só, guiada por scroll (`useScrollProgress` → canvas `sticky` por ~0,5 tela no desktop). Estado 1: 7 nós soltos (Site/Conteúdo/Anúncios/Leads/Atendimento/Dados + Noryos OS), conexões quebradas, pulsos que morrem no meio. Transição: nós deslizam pras posições do sistema (escalonado), conexões se completam, Noryos OS cresce + halo verde e assume o topo/centro (orquestrador). Estado final: losango — Noryos OS → canais → Leads → Atendimento → Dados, 3 pulsos percorrendo. Mobile: composição vertical própria (esmaecido+desalinhado+trilho-com-vão → alinhado+cheio), mais simples que o desktop por decisão. reduced-motion: zona colapsa, estado conectado estático. **Sem lib de animação** (avaliado Motion/GSAP, `useScrollProgress` + CSS resolve). `npm run quality` verde (**134 E2E**, +6 novos). Inspeção 1440/1280/1024/768/390/reduced-motion. **QUALITY GATE: APROVADO — VALIDADO LOCALMENTE.**

**Primitivos compartilhados** (`src/components/system/_primitives.tsx`): `useRevealOnce`, `IllustrativeCaption`, `SystemNode`, `Connection`, `ActivityToken` (este pausa o pulso fora do viewport). Extraídos dos dois usos reais (esteira + painel) + a nova seção. Esteira `.cv-node` mantida local de propósito (layout duplo + status + tooltip específicos).

**Resumo do redesign dos gráficos da home (04/09/2026):**
- **Automação** definiu o padrão de **fluxo/processamento** (esteira, token viajando, causa→efeito, loop calmo).
- **Aquisição e Performance** definiu o padrão de **leitura/performance** (revelação única no scroll, sem número absoluto, "Exemplo de acompanhamento").
- **Operação fragmentada→conectada** definiu o padrão de **transformação/sistema** (morph guiado por scroll, fragmentado→orquestrado).
- **Presença Digital** (`PresenceJourney`, substitui `BrowserPreview`) definiu o padrão de **jornada/conversão**: dois frames (browser + telefone) sincronizados, um "visitante" desce Hero→Prova→Oferta→Contato guiado pelo scroll; o Contato acende verde ("a página conduz até a ação"). Sem cursor falso, sem métrica. Mobile <~420px empilha os frames. reduced-motion = jornada concluída estática. `npm run quality` verde (**139 E2E**, +5 novos).
- **Conteúdo e Presença** (`ChannelHub`, substitui os 4 tiles idênticos) definiu o padrão de **distribuição/consistência multicanal**: hub "MENSAGEM CENTRAL" → 4 canais (Site/Instagram/Google/WhatsApp) com silhueta de formato abstrata (page/square/search/chat); revelação única no scroll — conexões traçam, formatos se adaptam, todos assumem o mesmo ciano ("uma estratégia, vários pontos de contato"). Sem carrossel/feed/post/logo. Mobile = 2×2 ao redor do hub. reduced-motion = estado conectado estático. `npm run quality` verde (**143 E2E**, +4 novos).
- **Noryos OS** (`NoryosOSExplorer` reescrito — mesmo nome/export) define o padrão de **workspace operacional**: janela com chrome (`NORYOS OS ··· operando`), 7 frentes de trabalho (Presença/Aquisição/Conteúdo/Automações/Dados/Documentação/Evolução) — desktop tablist (indicador que desliza, itens com stagger curto, setas navegam), mobile accordion (toque, sem hover, recolhe todas). Textos demonstrativos do método, sem inventar funcionalidade de produto; não é "file explorer". Nesse passe o **overflow mobile pré-existente de ~4px foi resolvido** (`main { overflow-x: clip }`, sticky-safe — vinha de `[data-anim="fade-right/left"]` na grade "Por que Noryos" + `ProcessTimeline`, não do Noryos OS). `npm run quality` verde (**146 E2E**, +3: rewrite dos testes do Noryos OS + teclado + reduced-motion + overflow mobile com motion ON). Inspeção 1440/1280/1024/768/390/reduced-motion/teclado/touch. **IMPLEMENTADO + TESTADO — aguardando aprovação visual do usuário** (o padrão ainda não conta como validado).
- Os cinco primeiros: **TESTADO + VALIDADO LOCALMENTE** (aprovados na inspeção visual). Redesign commitado; **produção: pendente** (sem deploy dessas mudanças).
- Depois da aprovação do Noryos OS: revisão da homepage inteira (quanto mudou, coerência entre os padrões de motion, ritmo entre seções, prioridade dos próximos ajustes) antes de qualquer P2.

## Prioridade principal

**O Formulário V2 do Diagnóstico Digital está VALIDADO EM PRODUÇÃO** (03/09/2026, commit `376a8c6`) — coleta estruturada (chips/cards) no ar, migração aditiva aplicada no Supabase, scoring V1 preservado, e-mail com bloco estruturado, leads antigos intactos, Turnstile confirmado. Bases do Diagnóstico (V1 + scoring V1 + Form V2) fechadas.

**Próximo foco: observar dados reais antes de evoluir.**

1. **Coletar leads reais** pelo `/diagnostico` (funil já instrumentado no `window.dataLayer`, sem PII).
2. **Medir conversão e drop-off por etapa** — comparar `visualizar_etapa` (1..5) com `diagnostico_enviado_com_sucesso`. Requer plugar o GTM/GA em produção (setar `NEXT_PUBLIC_GTM_ID` na env da Hostinger + redeploy; até lá os eventos ficam só no `dataLayer`, sem envio externo).
3. **Acumular base** de respostas estruturadas (`respostas` jsonb + `prazo`/`objetivo_principal`/`porte`) para, só depois, decidir e calibrar um **scoring v2** — nunca antes de ter dados reais suficientes. O V1 continua preservado (`scoring_version = "v1"`).
4. Candidatos parados na fila (só depois): prospecção ativa, blog/SEO, CRM/n8n.

Registros permanentes do Form V2: scoring V1 **preservado** · `form_version = v2` · `scoring_version = v1` · investimento **fora** do formulário · prazo e objetivo principal **obrigatórios** · porte **opcional** · analytics do funil **sem PII**.

Pendências humanas menores que seguem: setar `NEXT_PUBLIC_WHATSAPP_NUMBER=5561999256901` na env de produção da Hostinger + redeploy (o número já está no `.env.local`; **confirmado em 03/09/2026 que os CTAs em produção ainda caem no fallback `mailto:contato@noryosinovacoes.com.br`**) e plugar GTM/GA (`NEXT_PUBLIC_GTM_ID`) na env de produção + redeploy (**confirmado em 03/09/2026 que o HTML de produção ainda não injeta GTM/GA** — o funil fica só no `dataLayer`). Cache de CDN de HTML: **resolvido** (ver seção abaixo). (Logo/favicon: feitos em 29/08/2026.)

## Deploy — cache de CDN (resolvido em 03/09/2026)

Histórico: o HTML ficava **atrás do cache da CDN da Hostinger** com TTL longo e cada versão exigia **redeploy + purge manual de CDN no hPanel** pra aparecer (feito nos deploys de 01–03/09/2026). Mitigação no código: `next.config.mjs` serve `Cache-Control` curto pro documento HTML (commit `3eacc36`).

**No deploy do commit `a8b7bdc` (03/09/2026) a versão nova apareceu na URL limpa (`https://noryosinovacoes.com.br/`, sem query) sem purge manual** — resposta com `x-hcdn-cache-status: DYNAMIC`, `Cache-Control: public, max-age=0, s-maxage=30, must-revalidate` e `x-nextjs-cache: HIT`: a CDN parou de segurar o HTML. Assets estáticos seguem cacheados (comportamento correto). Confirmar em 1–2 deploys futuros que não volta a exigir purge.

## O que pode esperar

- Nicho odontologia (ainda não confirmado)
- CRM completo / integração com n8n
- Blog e páginas de conteúdo SEO
- Redes sociais e ads da própria Noryos (o site vem primeiro, por decisão do usuário)

## Contexto com prazo

Nenhum prazo definido ainda pelo usuário.
